= form_for @rsvp, remote: true, html: {class: @rsvp.attending} do |f|
  .inputs
    = f.hidden_field :attending, value: @rsvp.attending
    = f.label :name, @rsvp.name_message
    = f.text_field :name
    = f.label :comment, @rsvp.comment_message
    = f.text_area :comment, cols: 87, rows: 4
    .submit
      = f.submit 'SEND'

  #cancel
    X
